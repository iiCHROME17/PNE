# Table 5.3.1b — Pipeline Architecture: Prototype vs Current System

**Source:** CHANGELOG, `cognitive_thought_matcher.py`, `intention_registry.py`, `outcomes.py`

Each row shows one pipeline stage. Where a stage changed significantly between the prototype and the current system, the change and its motivation are noted.

| Stage | Prototype | Current System | Change & Reason |
|-------|-----------|----------------|-----------------|
| **I — NPC Intent** | JSON profile loaded; long-term desire and world context absent | Full three-block profile (`cognitive`, `social`, `world`) with personal history, known events, known figures, and faction | World context added 2 Mar 2026. NPCs now respond to political and social environment, not just immediate input. |
| **II — Choice Selection** | Free-text player input with no tone decomposition | Structured `PlayerDialogueInput` with discrete language art, four tone scores, skill dimension, ideology alignment tag | Tone decomposition enabled deterministic downstream scoring in cognitive and skill-check layers. |
| **IIa — Skill Check** | Threshold-only (player skill vs fixed NPC value) | Two-dice system (biased d6 × d6) + legacy threshold retained for attribute modifiers. `success_probability()` computed analytically. | Introduced stochastic uncertainty observable by the player, while keeping pre-roll probability analytically calculable for UI display. |
| **III — Cognitive Layer** | **LLM call #1** — `qwen2.5:3b` generated `internal_thought` and `subjective_belief` free-form (~4–8 s per turn) | **`CognitiveThoughtMatcher`** — deterministic template matching against 810 patterns in `cognitive_thoughts.json` (~sub-millisecond) | LLM outputs were too variable in phrasing and too slow for real-time play. Template matching is instantaneous and produces keyword-rich belief text the desire layer can reliably parse. |
| **IV — Desire Layer** | Simple valence-based lookup (positive → `affiliation`, negative → `protection`) | Six priority-ordered belief-keyword patterns + `BIAS_TO_DESIRE_MODIFIER` post-pattern correction | Richer desire differentiation. Two NPCs with the same valence but different bias types now produce different desires from the same input. |
| **V — Socialisation Filter** | Free-form intention string returned by LLM, passed to FSM as raw text | **`INTENTION_REGISTRY`** — closed canonical vocabulary; `SocialisationFilter` scores candidates by keyword overlap, confrontation band, and intensity | FSM routing failures caused by LLM phrasing variation. Fixed by guaranteeing every intention type is one of a finite set of registered names the FSM can reliably match. |
| **VI — Conversational Output** | **LLM call #2** — unconstrained prompt; min/max anchors absent; one outcome tier | Single LLM call with full BDI context, `min_response`/`max_response` anchors, skill-check pass/fail flag. Two-tier outcomes: `InteractionOutcome` (per-turn) + `TerminalOutcome` (scene-level) | Anchors constrain the generation envelope. Two-tier system separates turn consequences from narrative arc consequences, preventing abrupt scene termination. |
| **VII — Terminal Check** | Raw `player_relation` float compared to a threshold | `judgement_score` (0–100 integer, aggregated across turns) compared alongside `player_relation` | `player_relation` proved too volatile under small per-turn deltas. Judgement score provides a stable, accumulated signal that reflects the arc of the whole conversation. |

---

**Summary:**

| Metric | Prototype | Current |
|--------|-----------|---------|
| LLM calls per turn | **2** | **1** |
| Cognitive processing time | ~4–8 s | ~0 ms |
| Intention vocabulary | Open / free-form | Closed / 19 canonical types |
| FSM routing reliability | Non-deterministic | Deterministic |
| Desire differentiation | 2 types (valence-only) | 4 types × 11 bias modifiers |
| Terminal routing signal | Raw float | Aggregated integer (0–100) |
